<%= form_with(model: trigger) do |form| %>
  <% if trigger.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(trigger.errors.count, "error") %> prohibited this trigger from being saved:</h2>

      <ul>
        <% trigger.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :type_id, style: "display: block" %>
    <%= form.text_field :type_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
