<%= form_with(model: scheduling) do |form| %>
  <% if scheduling.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(scheduling.errors.count, "error") %> prohibited this scheduling from being saved:</h2>

      <ul>
        <% scheduling.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :account_id, style: "display: block" %>
    <%= form.text_field :account_id %>
  </div>

  <div>
    <%= form.label :device_id, style: "display: block" %>
    <%= form.text_field :device_id %>
  </div>

  <div>
    <%= form.label :group_id, style: "display: block" %>
    <%= form.text_field :group_id %>
  </div>

  <div>
    <%= form.label :schedule_time, style: "display: block" %>
    <%= form.datetime_field :schedule_time %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
